# 部署到 GitHub Pages

1. 將本壓縮檔解壓縮。
2. 把解壓縮後的全部檔案與資料夾上傳到 `lawyerhao83-maker/home-next-family-law` 的 `main` 分支。
3. 打開倉庫的 `Settings` → `Pages`。
4. 在 `Build and deployment` 的 `Source` 選擇 `GitHub Actions`。
5. 等候 `Actions` 頁面中的部署完成。

網站網址：

`https://lawyerhao83-maker.github.io/home-next-family-law/`

若日後將倉庫重新命名為 `family-law`，重新執行一次部署後，網址會變成：

`https://lawyerhao83-maker.github.io/family-law/`
